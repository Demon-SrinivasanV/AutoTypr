# AutoTypr
AutoTypr is an AutoHotkey script that simulates keyboard input to type the contents of a text file into any active window — perfect for VDIs, remote desktops, and SSH sessions where clipboard sharing isn't available.
